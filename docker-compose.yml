services:
  material-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: material_back
    env_file:
      - .env
    ports:
      - "${FASTAPI_PORT:-9876}:9876"
    restart: unless-stopped
